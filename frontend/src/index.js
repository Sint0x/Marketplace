import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// рендерит элементы из App.js в index.html по id root
ReactDOM.render(<App />, document.getElementById('root'));
